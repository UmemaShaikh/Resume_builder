import React from 'react';
import './welcome.css';  // Ensure the CSS file is correctly linked

const WelcomeMessage = () => {
    return (
        <div className="welcome-message">
            <h2 className="typing">Welcome to the Register Page!</h2>
        </div>
    );
}

export default WelcomeMessage;

